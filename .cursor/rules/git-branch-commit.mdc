---
description: GitHub 分支命名与提交备注（commit/PR）规范；创建分支、提交、开 PR 时必须遵守
alwaysApply: true
---

# Git 分支与提交规范

创建分支、撰写 commit、开 PR 时遵循本规范。详细流程见 skill `git-naming-conventions`。

## 分支命名

格式：`<type>/<optional-ticket>-<short-desc>`

| type | 用途 |
|------|------|
| `feature` | 新功能 |
| `fix` | 非紧急缺陷 |
| `hotfix` | 生产紧急修复（从默认分支拉出） |
| `test` | 测试/部署验证 |
| `refactor` | 重构（行为不变） |
| `docs` | 文档 |
| `chore` | 构建、依赖、工具 |
| `release` | 发版准备 |

规则：
- 全小写；单词用 `-`（kebab-case）；禁止空格、下划线、驼峰
- `short-desc` 简洁、可读，建议 ≤ 5 个词
- 有 Issue/工单时：`feature/123-add-search` 或 `fix/WP-45-null-npe`
- **禁止**直接向默认分支（`main`/`master`）推送；一律 PR + CI

```
✅ feature/integration-ontology
✅ fix/834-login-redirect
✅ hotfix/security-session-leak
✅ test/deployment-readiness
❌ Feature/IntegrationOntology
❌ feature_add_search
❌ ArchitectureEnhancement
```

## 提交备注（Conventional Commits）

```
<type>(<optional-scope>): <subject>

[optional body]

[optional footer]
```

常用 type：`feat` | `fix` | `docs` | `refactor` | `test` | `chore` | `perf` | `ci` | `build` | `revert`

规则：
- subject 祈使语气、现在时；不加句号；建议 ≤ 72 字符
- 说明 **why**，不要堆砌文件列表
- 关联 Issue：`Closes #123` / `Fixes #123`
- 破坏性变更：footer 写 `BREAKING CHANGE: ...`，或 type 后加 `!`

```
✅ feat(search): add Lucene deprecated-entity index
✅ fix(auth): prevent session leak on logout

Closes #834
❌ update
❌ Fixed stuff.
❌ WIP
```

## PR 标题

与 commit 同格式：`feat(search): add Lucene deprecated-entity index`

## 快速决策

| 场景 | 分支 type | commit type |
|------|-----------|-------------|
| 新能力 | `feature/` | `feat` |
| 修 bug | `fix/` | `fix` |
| 线上紧急 | `hotfix/` | `fix` |
| 仅文档 | `docs/` | `docs` |
| 重构无行为变 | `refactor/` | `refactor` |
| 依赖/脚本 | `chore/` | `chore` / `ci` / `build` |
