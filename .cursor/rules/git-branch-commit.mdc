---
description: GitHub 分支命名、commit/PR 标题与 GitHub Comment（Issue/PR/Review）提交规范；建分支、提交、开 PR、发评论时必须遵守
alwaysApply: true
---

# Git 分支、提交与 Comment 规范

创建分支、撰写 commit、开 PR、在 GitHub 发 Issue/PR/Review 评论时遵循本规范。详细流程见 skill `git-naming-conventions`。

## 分支命名

格式：`<type>/<optional-ticket>-<short-desc>`

| type | 用途 |
|------|------|
| `feature` | 新功能 |
| `fix` | 非紧急缺陷 |
| `hotfix` | 生产紧急修复（从默认分支拉出） |
| `test` | 测试/部署验证 |
| `refactor` | 重构（行为不变） |
| `docs` | 文档 |
| `chore` | 构建、依赖、工具 |
| `release` | 发版准备 |

规则：
- 全小写；单词用 `-`（kebab-case）；禁止空格、下划线、驼峰
- `short-desc` 简洁、可读，建议 ≤ 5 个词
- 有 Issue/工单时：`feature/123-add-search` 或 `fix/WP-45-null-npe`
- **禁止**直接向默认分支（`main`/`master`）推送；一律 PR + CI

```
✅ feature/integration-ontology
✅ fix/834-login-redirect
✅ hotfix/security-session-leak
✅ test/deployment-readiness
❌ Feature/IntegrationOntology
❌ feature_add_search
❌ ArchitectureEnhancement
```

## 提交备注（Conventional Commits）

```
<type>(<optional-scope>): <subject>

[optional body]

[optional footer]
```

常用 type：`feat` | `fix` | `docs` | `refactor` | `test` | `chore` | `perf` | `ci` | `build` | `revert`

规则：
- subject 祈使语气、现在时；不加句号；建议 ≤ 72 字符
- 说明 **why**，不要堆砌文件列表
- 关联 Issue：`Closes #123` / `Fixes #123`
- 破坏性变更：footer 写 `BREAKING CHANGE: ...`，或 type 后加 `!`

```
✅ feat(search): add Lucene deprecated-entity index
✅ fix(auth): prevent session leak on logout

Closes #834
❌ update
❌ Fixed stuff.
❌ WIP
```

## PR 标题

与 commit 同格式：`feat(search): add Lucene deprecated-entity index`

## GitHub Comment（Issue / PR / Review）

采用 Conventional Comments：

```
<label> [optional decorations]: <subject>

[optional body — evidence, why, suggested fix]
```

| label | 用途 |
|-------|------|
| `praise` | 肯定值得保留的写法 |
| `nitpick` | 琐碎偏好（默认非阻塞） |
| `suggestion` | 具体改进建议 |
| `issue` | 缺陷 / 不正确行为（默认阻塞） |
| `question` | 需作者澄清 |
| `todo` | 合并前必须完成的小项 |
| `thought` | 非要求的想法 |
| `chore` | 流程 / CI / 杂项提醒 |
| `note` | 中性说明 |

Decorations（可省略）：`(non-blocking)` `(blocking)` `(if-minor)` `(security)` `(perf)`

规则：
- 一条评论一个关注点；先结论后证据
- 行内 Review 优先给可落地建议（必要时附代码块）
- `issue` / `todo` 默认阻塞合并；`nitpick` / `thought` / `praise` 默认非阻塞
- 禁止：仅 “LGTM”/“+1”、无上下文的 “fix this”、人身攻击或嘲讽
- 用 `gh` 发评论时正文走 HEREDOC，保持上述格式

```
✅ suggestion: extract null-check into Optional to match nearby handlers

The surrounding index code already uses Optional; this keeps the NPE path consistent.

✅ issue (blocking): session is not invalidated before cookie clear

See AuthFilter.java logout path — concurrent requests can reuse the session.

❌ LGTM
❌ fix this
❌ 这里写得不行
```

## 快速决策

| 场景 | 分支 type | commit type |
|------|-----------|-------------|
| 新能力 | `feature/` | `feat` |
| 修 bug | `fix/` | `fix` |
| 线上紧急 | `hotfix/` | `fix` |
| 仅文档 | `docs/` | `docs` |
| 重构无行为变 | `refactor/` | `refactor` |
| 依赖/脚本 | `chore/` | `chore` / `ci` / `build` |
